/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
    domains: ['cbucxmuxpflptgjzawep.supabase.co'], // ← your Supabase project domain
  },
};

export default nextConfig;
